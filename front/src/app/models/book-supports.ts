import { LoadableModel } from './loadable';

export class BookSupportsModel extends LoadableModel {
    public readonly name = 'Book Supports';
    public readonly path = 'models/book_supports.glb';
}
